# Gate Room and Walkable Step Fix

Fixes:
1. Gate is now enclosed like a small room instead of only having a thin door frame.
2. Added left/right room walls, partial back walls, and a roof block around the gate.
3. Replaced the previous angled step with low flat walkable platforms.
4. Because jumping is disabled, the threshold is now reachable by walking directly forward.
