# Gate Step Ground Contact Fix

Fix:
1. Gate threshold and platforms are moved up to touch the floor instead of floating/sinking too low.
2. Step height is reduced so it can be walked over without jumping.
3. A thin floor bridge is added across the doorway so the player can smoothly walk into the gate room.
4. This keeps jump disabled while making the exit path reachable.
