# Cartoon UI Font and Reflection Counter Update

Changes:
1. Added CartoonGUI helper for all main GUI.Box prompts.
2. Prompt panels use a cartoon-style dynamic font fallback list.
3. Prompt text now uses word wrap and larger panels to avoid text overflowing.
4. Inventory HUD uses the same cartoon style.
5. Inventory HUD now shows mirror reflection progress:
   Mirrors: reflected / required
6. Interaction prompts for mirrors, rails, switches, EMP, pickup messages, and level messages use the same style.
